---
'@goodnight-dev/react-hooks': minor
---

Add `useLocalStorage`, a `useSyncExternalStore`-backed hook for reading and
writing a JSON-serializable value in `localStorage`, staying in sync across
tabs.
